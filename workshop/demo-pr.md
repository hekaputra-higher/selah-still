# Add a remaining-character hint to D.W.E.L.L.

People need to know how much room remains before reaching the response limit. This change shows a quiet character hint under the active response field while preserving navigation, writing and saving.

Validation to fill with actual evidence after the change: formatting, lint, types, response-limit behaviour, keyboard/screen reader review, mobile preview and production build. Do not prefill passing results.

Scope: no database or authorization changes. Review the Vercel Preview for the exact PR commit before merging.
